<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" type="text/css" href="/stylesheets/index.css" />
    <title>Deleting Category - Inventory App</title>
  </head>
  <body>
    <div id="main-container">
      <div id="sidebar">
        <h1>Imaginary Store</h1>
        <h2>Categories Manager</h2>
        <a href="/categories/new">📃 Add Categories</a>
        <a href="/categories/edit">📝 Edit Categories</a>
        <a href="/categories/delete">🗑️ Delete Categories</a>
        <h2>Disclaimer</h2>
        <p>
          To prevent shenanigans, you can only add, edit, or delete items if you
          have an admin password.
        </p>
      </div>

      <div id="inventory-container">
        <h1>
          Warning: deleting a category will also delete all items that belong to
          that category.
        </h1>
        <p>
          i.e: deleting "Fruit" will delete both "Banana" and "Lemon" from your
          inventory.
        </p>

        <form method="POST" action="/categories?_method=DELETE">
          <div class="input-container">
            <label>Choose Category To Delete*</label>
            <select id="categoryName" name="categoryName" required>
              <option value="" disabled selected>Select a category...</option>
              <% categoriesArray.forEach(function(category) { %>
              <option value="<%= category %>"><%= category %></option>
              <% }) %>
            </select>
          </div>

          <button id="form-button" type="submit">Submit</button>
        </form>

        <a class="return" href="/">🔙 Return to inventory</a>
      </div>
    </div>
  </body>
</html>
